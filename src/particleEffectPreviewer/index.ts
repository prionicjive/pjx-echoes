import { ParticleEffectPreviewer } from './ParticleEffectPreviewer.ts'; // Main game controller (handles everything!)

const previewer = new ParticleEffectPreviewer(); // Create a new previewer instance
previewer.init();    // Initialize and start the loop